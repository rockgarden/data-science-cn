# =============================================================================
# 核心数据科学库
# =============================================================================

# 数据处理与分析
pandas~=2.1.0               # 强大的数据结构和数据分析工具
pandas_ta~=0.4.0              # Pandas 的技术分析库，提供多种金融指标和策略
pyarrow~=14.0               # Apache Arrow 的 Python 绑定，用于高效数据读写（如 Parquet）
numpy>=1.21.0

# 数据可视化
matplotlib~=3.8.0           # 基础绘图库，支持高质量图表输出
seaborn~=0.13.0             # 基于 matplotlib 的高级统计图表库，美观易用
scikit-image~=0.22.0        # 图像处理工具库，支持多种图像算法

# 机器学习与建模
scikit-learn~=1.3.0         # 经典机器学习库，包含分类、回归、聚类等算法

# 图像处理
opencv-python~=4.8.0        # OpenCV 的 Python 接口，用于计算机视觉任务
pillow~=10.0.0              # Python 图像处理库（PIL 分支），支持图像打开、裁剪、滤镜等

# 数据验证
pandera~=0.16.0             # 用于 pandas DataFrame 的数据类型和数据质量验证

# 金融数据获取
tvdatafeed>=1.3.1
yfinance>=0.2.30

# 回测框架
vectorbt>=1.4.2

# =============================================================================
# Jupyter 交互式开发环境
# =============================================================================

# Jupyter 基础运行环境
jupyter~=1.0                # Jupyter Notebook 启动和管理工具
jupyterlab~=4.0.0           # 下一代 Jupyter 用户界面，模块化、支持扩展
ipywidgets~=8.1.0           # 在 Jupyter 中创建交互式控件（如滑块、按钮）

# 代码格式化工具（自动 PEP8）
autopep8                    # 自动格式化 Python 代码以符合 PEP8 规范
jupyterlab_code_formatter # JupyterLab 插件，支持在界面中一键格式化代码


# =============================================================================
# 工具与辅助库
# =============================================================================

# 任务自动化工具（类似 Makefile 的 Python 实现）
invoke~=1.7               # 用于定义和运行重复性任务（如清理、测试、部署）

# 数据探索与分析报告
ydata-profiling~=4.6.0    # 自动生成数据集的详细分析报告（含缺失值、分布、相关性等）


# =============================================================================
# 关于 Graphviz 的重要说明
# =============================================================================

# Python 接口：用于生成和渲染图形（如决策树、流程图）
graphviz~=0.20.0

# ⚠️ 注意：该包仅提供 Python 调用接口
# 你必须**单独安装 Graphviz 系统软件**才能正常渲染图形！
#
# 安装方式：
#   - macOS: 使用 Homebrew 安装：brew install graphviz
#   - Ubuntu/Debian: sudo apt-get install graphviz
#   - Windows: 从官网下载安装程序：https://www.graphviz.org/download/
#     并确保将 Graphviz 的 bin 目录添加到系统 PATH 环境变量中
#
# 验证方法：
#   在 Python 中运行以下代码测试是否成功：
#   import graphviz
#   dot = graphviz.Digraph()
#   dot.node('A', '测试')
#   dot.node('B', '图形')
#   dot.edge('A', 'B')
#   dot.render('test', format='png', view=True)